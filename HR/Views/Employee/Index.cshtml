@model IEnumerable<HR.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Insert")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Branche.BranchesName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Department.DeprtName)
        </th>
        <th></th>
    </tr>
    
@foreach (var employee in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => employee.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => employee.Address)
        </td>
        <td>            
            @Html.DisplayFor(modelItem => employee.Branche.BranchesName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => employee.Department.DeprtName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=employee.Id }) |
            @Html.ActionLink("Details", "Details", new { id=employee.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=employee.Id })
        </td>
    </tr>
}

</table>
